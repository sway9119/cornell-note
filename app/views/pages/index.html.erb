<p style="color: green"><%= notice %></p>

<h1>Pages</h1>

<div id="pages">
  <ul>
    <% @pages.each do |page| %>
      <li><%= link_to page.keyword, page_path(page.id) %></li>
    <% end %>
  </ul>
</div>

<%= link_to "New page", new_page_path %>
